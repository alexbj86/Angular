import { LogInputsDirective } from './log-inputs.directive';

describe('LogInputsDirective', () => {
  it('should create an instance', () => {
    const directive = new LogInputsDirective();
    expect(directive).toBeTruthy();
  });
});
